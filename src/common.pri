BUILD_DIR = $${ROOT_DIR}/build

OBJECTS_DIR = $${BUILD_DIR}/obj
MOC_DIR = $${BUILD_DIR}/moc
UI_DIR = $${BUILD_DIR}/ui
RCC_DIR = $${BUILD_DIR}/rcc

#-----------

LIB_PATH = $${ROOT_DIR}/bin/lib
BIN_PATH = $${ROOT_DIR}/bin
